# Pinned 2026-05-30 — version locked cho Streamlit Cloud reproducible build.
# vnstock 4.x mở khoá Báo cáo tài chính real (income/balance/cashflow).
# v58: bỏ matplotlib (sparkline đã chuyển SVG pure Python) — tiết kiệm 80MB
# build + 3-5s khởi động trên Streamlit Cloud free tier (1GB RAM).
streamlit==1.39.0
streamlit-option-menu==0.3.13
pandas==2.3.3
numpy==2.2.6
plotly==5.24.1
scipy==1.14.1
scikit-learn==1.5.2
statsmodels==0.14.4
arch==8.0.0
vnstock==4.0.4
requests==2.32.3

# Đọc hiểu tin tức bằng HỌC SÂU (PhoBERT sentiment tiếng Việt) — TÙY CHỌN.
# Bật toggle "Đọc hiểu bằng AI học sâu" ở trang Tin tức cần 2 gói dưới.
# KHÔNG cài (mặc định) → app tự dùng từ điển tài chính (deploy-safe, luôn chạy).
# Cài nặng (~1.5GB) — chỉ bỏ '#' khi máy/deploy đủ RAM:
# torch==2.4.1
# transformers==4.44.2
